import 'package:flutter/material.dart';

const COLOR_DARK_BLUE = Color(0xFF1F2533);
const COLOR_WHITE = Colors.white;
const COLOR_PINKISH_RED = Color(0xFFF84464);

const TextTheme TEXT_THEME_DEFAULT = TextTheme(
  headline1:
      TextStyle(color: COLOR_WHITE, fontWeight: FontWeight.w700, fontSize: 26),
  headline2:
      TextStyle(color: COLOR_WHITE, fontWeight: FontWeight.w200, fontSize: 12),
);

const TextTheme TEXT_THEME_SMALL = TextTheme(
  headline1:
      TextStyle(color: COLOR_WHITE, fontWeight: FontWeight.w700, fontSize: 22),
  headline2:
      TextStyle(color: COLOR_WHITE, fontWeight: FontWeight.w200, fontSize: 10),
);
